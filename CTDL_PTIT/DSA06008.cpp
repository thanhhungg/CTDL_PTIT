#include <bits/stdc++.h>
using namespace std;
#define endl "\n"
void sloved(int a[],int n,int b[],int m)
{
    if( )
}
int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cou.tie(0);
    int t;
    cin >> t;
    while (t--)
    {
        int n,m;
        cin>>n>>m;
        int a[n],b[m];
        for(int i = 0; i < n; i++)
        {
            cin>>a[i];
        }
        for(int i=0;i<m; i++)
        {
            cin>>b[i];
        }
        solved(a,n,b,m);
    }
}